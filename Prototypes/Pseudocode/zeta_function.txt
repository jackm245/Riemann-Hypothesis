FUNCTION zeta(r, t)
  IF NOT r > 0 THEN
    OUTPUT ValueError
  ELSE
    TERMS 🠀 1*10**6
    Re 🠀 0
    Im 🠀 0
    FOR n 🠀 0 TO TERMS
      A 🠀 (-1)**(n-1) / n**r
      Phit 🠀 log(n) * t
      Re 🠀 Re + A *cos(Phit)
      Im 🠀 Im - A*sin(Phit)
    ENDFOR
    eta 🠀 Re + Im
    x 🠀 -2**(1-r)
    y 🠀 2**(1j*t)
    zeta_over_eta 🠀 y / (x+y)
    zeta 🠀 zeta_over_eta * eta
    OUTPUT zeta
  ENDIF
ENDFUNCTION


